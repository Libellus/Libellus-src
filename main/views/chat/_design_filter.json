{
   "_id": "_design/filter",
   "language": "javascript",
   "views": {
       "all": {
           "map": "function(doc) { if (doc.type && (doc.type == \"chat\")) emit(doc._id, doc) }"
       }
   }
}

